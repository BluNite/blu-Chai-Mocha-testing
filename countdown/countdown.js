// print all numbers from 'num' down to 1
//  assume `num` will be a positive number
// var countdown = func(num)
var countdown = function (num) {
	// for loop length is set to 'num' decrease by 1
	for (var i = num; i > 0; i--) {
		// logs num loop 
		console.log(i);
	}
};